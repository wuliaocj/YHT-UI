# 益禾堂奶茶点餐系统后端开发文档

## 1. 项目结构与模块划分

### 1.1 核心模块
- **用户模块**：用户认证、信息管理
- **商品模块**：商品管理、分类管理
- **订单模块**：订单创建、管理、状态更新
- **购物车模块**：商品加入、更新、删除
- **地址模块**：地址管理
- **支付模块**：支付创建、状态查询
- **上传模块**：图片上传
- **管理模块**：后台管理接口

### 1.2 技术栈
- **后端框架**：Spring Boot 3.5.10
- **数据库**：MySQL
- **ORM**：MyBatis + MyBatis-Plus
- **认证**：JWT + Spring Security
- **接口文档**：SpringDoc OpenAPI (Swagger 3.0)

## 2. API接口文档

### 2.1 基础信息
- **API前缀**：`/api`
- **后端端口**：`8080`
- **认证方式**：Bearer Token (JWT)
- **响应格式**：统一JSON格式

### 2.2 核心接口

#### 用户认证
| 接口路径 | 方法 | 类型 | 功能描述 | 请求参数 | 成功返回结构 |
|---------|------|------|---------|---------|-------------|
| `/api/user/login` | POST | 公开 | 微信小程序登录 | `{ "code": "微信登录码" }` | `{ "token": "JWT令牌", "openid": "用户OpenID", "userInfo": { "nickname": "昵称", "avatar": "头像URL" } }` |
| `/api/user/checkToken` | GET | 需要认证 | 检查Token有效性 | N/A | `{ "code": 200, "msg": "token 有效", "data": null }` |

#### 商品管理
| 接口路径 | 方法 | 类型 | 功能描述 | 请求参数 | 成功返回结构 |
|---------|------|------|---------|---------|-------------|
| `/api/product/{id}` | GET | 公开 | 获取商品详情 | `id` (路径参数) | `{ "code": 200, "msg": "查询成功", "data": 商品详情 }` |
| `/api/product/user/list` | GET | 公开 | 获取用户商品列表 | N/A | `{ "code": 200, "msg": "用户商品查询成功", "data": [商品列表] }` |
| `/api/product/admin/add` | POST | 管理员 | 添加商品 | 商品详情JSON | `{ "code": 200, "msg": "商品添加成功" }` |
| `/api/product/admin/list` | GET | 管理员 | 获取后台商品列表 | N/A | `{ "code": 200, "msg": "商品查询成功", "data": [商品列表] }` |
| `/api/product/admin/update` | POST | 管理员 | 更新商品 | 商品详情JSON | `{ "code": 200, "msg": "商品修改成功", "data": 更新结果 }` |
| `/api/product/admin/delete/{id}` | POST | 管理员 | 删除商品 | `id` (路径参数) | `{ "code": 200, "msg": "商品删除成功" }` |

#### 购物车
| 接口路径 | 方法 | 类型 | 功能描述 | 请求参数 | 成功返回结构 |
|---------|------|------|---------|---------|-------------|
| `/api/cart/{userId}` | GET | 需要认证 | 获取用户购物车 | `userId` (路径参数) | `{ "code": 200, "msg": "success", "data": [购物车商品] }` |
| `/api/cart/add` | POST | 需要认证 | 加入购物车 | `AddCartVO` JSON | `{ "code": 200, "msg": "加入购物车成功", "data": 购物车商品 }` |
| `/api/cart/update` | POST | 需要认证 | 更新购物车商品 | `cartId`, `quantity`, `selectedSpecs`, `selected` | `{ "code": 200, "msg": "success" }` |
| `/api/cart/remove/{cartId}` | POST | 需要认证 | 删除购物车商品 | `cartId` (路径参数) | `{ "code": 200, "msg": "success" }` |
| `/api/cart/clear/{userId}` | POST | 需要认证 | 清空购物车 | `userId` (路径参数) | `{ "code": 200, "msg": "success" }` |

#### 订单
| 接口路径 | 方法 | 类型 | 功能描述 | 请求参数 | 成功返回结构 |
|---------|------|------|---------|---------|-------------|
| `/api/order/create` | POST | 需要认证 | 创建订单 | `userId` (查询参数), 订单详情JSON | `{ "code": 200, "msg": "success", "data": 创建的订单 }` |
| `/api/order/list/{userId}` | GET | 需要认证 | 获取用户订单列表 | `userId` (路径参数) | `{ "code": 200, "msg": "success", "data": [订单列表] }` |
| `/api/order/{orderId}` | GET | 需要认证 | 获取订单详情 | `orderId` (路径参数) | `{ "code": 200, "msg": "success", "data": 订单详情 }` |
| `/api/order/admin/order/list` | GET | 公开 | 管理员获取订单列表 | 分页参数 | `{ "code": 200, "msg": "success", "data": 分页订单列表 }` |
| `/api/order/admin/order/update` | POST | 管理员 | 管理员更新订单 | `{ "orderId": 订单ID, "status": 状态, "adminRemark": 备注 }` | `{ "code": 200, "msg": "更新成功" }` |

#### 地址
| 接口路径 | 方法 | 类型 | 功能描述 | 请求参数 | 成功返回结构 |
|---------|------|------|---------|---------|-------------|
| `/api/address/list/{userId}` | GET | 需要认证 | 获取用户地址列表 | `userId` (路径参数) | `{ "code": 200, "msg": "success", "data": [地址列表] }` |
| `/api/address/save` | POST | 需要认证 | 保存地址 | 地址详情JSON | `{ "code": 200, "msg": "success", "data": 保存的地址 }` |
| `/api/address/delete/{id}` | POST | 需要认证 | 删除地址 | `id` (路径参数) | `{ "code": 200, "msg": "success" }` |

#### 支付
| 接口路径 | 方法 | 类型 | 功能描述 | 请求参数 | 成功返回结构 |
|---------|------|------|---------|---------|-------------|
| `/api/payment/create` | POST | 需要认证 | 创建支付单 | `orderNo` (查询参数) | `{ "code": 200, "msg": "success", "data": 支付参数 }` |
| `/api/payment/status/{orderNo}` | GET | 需要认证 | 查询支付状态 | `orderNo` (路径参数) | `{ "code": 200, "msg": "success", "data": 支付记录 }` |
| `/api/payment/mock/success` | POST | 需要认证 | 模拟支付成功 | `orderNo` (查询参数) | `{ "code": 200, "msg": "模拟支付成功" }` |

#### 上传
| 接口路径 | 方法 | 类型 | 功能描述 | 请求参数 | 成功返回结构 |
|---------|------|------|---------|---------|-------------|
| `/api/upload/image` | POST | 公开 | 多图片上传 | `files` (FormData文件数组) | `{ "code": 200, "msg": "success", "data": [图片URL列表] }` |

## 3. 数据模型

### 3.1 核心实体

#### User (用户)
| 字段名 | 类型 | 描述 |
|-------|------|------|
| id | Integer | 用户ID |
| openid | String | 微信OpenID |
| unionid | String | 微信UnionID |
| nickname | String | 昵称 |
| avatarUrl | String | 头像URL |
| phone | String | 手机号 |
| gender | Integer | 性别 |
| province | String | 省份 |
| city | String | 城市 |
| integral | Integer | 积分 |
| vipLevel | Integer | VIP等级 |
| totalConsumption | BigDecimal | 总消费金额 |
| lastLoginTime | LocalDateTime | 最后登录时间 |
| status | Integer | 状态 |
| createTime | LocalDateTime | 创建时间 |
| updateTime | LocalDateTime | 更新时间 |

#### Product (商品)
| 字段名 | 类型 | 描述 |
|-------|------|------|
| id | Long | 商品ID (雪花算法) |
| categoryId | Integer | 分类ID |
| name | String | 商品名称 |
| enName | String | 英文名称 |
| description | String | 描述 |
| detail | String | 详情 |
| mainImage | String | 主图 |
| images | String | 图片列表 |
| basePrice | BigDecimal | 基础价格 |
| originPrice | BigDecimal | 原价 |
| inventory | Integer | 库存 |
| salesCount | Integer | 销量 |
| isHot | Integer | 是否热销 |
| isNew | Integer | 是否新品 |
| isRecommend | Integer | 是否推荐 |
| customOptions | String | 自定义选项 |
| status | Integer | 状态 |
| sortOrder | Integer | 排序 |
| createTime | LocalDateTime | 创建时间 |
| updateTime | LocalDateTime | 更新时间 |

#### Order (订单)
| 字段名 | 类型 | 描述 |
|-------|------|------|
| id | Integer | 订单ID |
| orderNo | String | 订单号 |
| userId | Integer | 用户ID |
| totalAmount | BigDecimal | 总金额 |
| discountAmount | BigDecimal | 优惠金额 |
| deliveryFee | BigDecimal | 配送费 |
| actualAmount | BigDecimal | 实际金额 |
| paymentMethod | Integer | 支付方式 |
| paymentStatus | Integer | 支付状态 |
| paymentTime | LocalDateTime | 支付时间 |
| transactionId | String | 交易ID |
| orderStatus | Integer | 订单状态 |
| orderType | Integer | 订单类型 |
| takeCode | String | 取餐码 |
| estimatedTime | LocalDateTime | 预计时间 |
| completeTime | LocalDateTime | 完成时间 |
| cancelReason | String | 取消原因 |
| cancelTime | LocalDateTime | 取消时间 |
| userRemark | String | 用户备注 |
| adminRemark | String | 管理员备注 |
| createTime | LocalDateTime | 创建时间 |
| updateTime | LocalDateTime | 更新时间 |

#### Cart (购物车)
| 字段名 | 类型 | 描述 |
|-------|------|------|
| id | Integer | 购物车ID |
| userId | Integer | 用户ID |
| productId | Long | 商品ID |
| productName | String | 商品名称 |
| productImage | String | 商品图片 |
| productPrice | BigDecimal | 商品价格 |
| quantity | Integer | 数量 |
| selectedSpecs | String | 选择的规格 |
| selected | Boolean | 是否选中 |
| createTime | LocalDateTime | 创建时间 |
| updateTime | LocalDateTime | 更新时间 |

#### Address (地址)
| 字段名 | 类型 | 描述 |
|-------|------|------|
| id | Integer | 地址ID |
| userId | Integer | 用户ID |
| receiver | String | 收件人 |
| phone | String | 手机号 |
| province | String | 省份 |
| city | String | 城市 |
| district | String | 区县 |
| addressDetail | String | 详细地址 |
| isDefault | Integer | 是否默认 |
| createTime | LocalDateTime | 创建时间 |
| updateTime | LocalDateTime | 更新时间 |

## 4. 认证与授权

### 4.1 认证流程
1. **前端**：调用微信登录API获取code
2. **前端**：将code发送到后端登录接口 `/api/user/login`
3. **后端**：使用code调用微信接口获取openid，查询/创建用户，生成JWT token
4. **前端**：保存token到本地存储
5. **前端**：后续请求在请求头中携带 `Authorization: Bearer {token}`

### 4.2 权限控制
- **公开接口**：无需认证，如登录、商品列表、支付回调
- **用户接口**：需要USER角色，如订单管理、购物车操作
- **管理员接口**：需要ADMIN角色，如商品管理、订单管理

## 5. 特殊功能实现

### 5.1 文件上传
- **支持多图片上传**：通过FormData发送文件数组
- **支持格式**：jpg, jpeg, png, webp
- **大小限制**：2MB/文件
- **返回**：可访问的图片URL列表

### 5.2 支付流程
1. **创建订单**：调用 `/api/order/create`
2. **创建支付**：调用 `/api/payment/create` 获取支付参数
3. **发起支付**：前端调用微信支付API
4. **查询状态**：支付后调用 `/api/payment/status/{orderNo}` 查询支付状态
5. **模拟支付**：测试环境可调用 `/api/payment/mock/success` 模拟支付成功

### 5.3 微信小程序登录
- **使用微信官方登录流程**：code2Session获取openid
- **自动注册**：首次登录自动创建用户
- **返回用户信息**：登录成功后返回用户基本信息

## 6. 前端开发注意事项

### 6.1 API调用规范
- **基础URL**：`http://localhost:8080/api` (开发环境)
- **请求头**：携带Authorization token
- **错误处理**：统一处理401(未认证)、403(无权限)等错误
- **参数校验**：前端应进行必要的参数校验，减少无效请求

### 6.2 认证管理
- **Token存储**：使用localStorage或sessionStorage存储token
- **Token刷新**：监控token有效期，必要时重新登录
- **登出处理**：清除本地token，跳转到登录页面

### 6.3 数据处理
- **时间格式**：后端返回LocalDateTime，前端需处理时间格式化
- **金额处理**：使用BigDecimal存储金额，前端需注意精度
- **图片路径**：使用返回的完整URL访问图片

### 6.4 开发环境配置
- **微信开发者工具**：开启"不校验合法域名"选项
- **跨域处理**：后端已配置CORS，前端无需额外处理
- **接口调试**：可使用Swagger UI (`/swagger-ui.html`) 调试接口

### 6.5 测试建议
- **使用模拟数据**：开发初期使用模拟数据
- **单元测试**：对核心功能进行单元测试
- **集成测试**：测试完整业务流程
- **性能测试**：测试高并发场景下的性能

## 7. 常见问题与解决方案

### 7.1 认证问题
- **Token过期**：重新登录获取新token
- **Token无效**：检查token格式和完整性
- **权限不足**：确认用户角色和接口权限

### 7.2 支付问题
- **支付失败**：检查订单状态和支付参数
- **支付回调**：生产环境需配置正确的回调地址
- **订单状态**：支付后及时更新订单状态

### 7.3 上传问题
- **文件过大**：压缩图片后再上传
- **格式错误**：确保上传支持的图片格式
- **路径错误**：使用返回的完整URL访问图片

## 8. 部署与上线

### 8.1 环境配置
- **数据库**：创建对应数据库，导入 `益禾堂奶茶点餐系统数据库设计.sql`
- **配置文件**：修改 `application.properties` 中的数据库连接信息
- **微信配置**：修改微信小程序和支付的AppID、Secret等配置

### 8.2 部署方式
- **打包**：`mvn clean package`
- **运行**：`java -jar target/demo-0.0.1-SNAPSHOT.jar`
- **Docker**：可使用Docker容器化部署

### 8.3 监控与维护
- **日志**：查看应用日志定位问题
- **监控**：建议使用Spring Boot Actuator监控应用状态
- **备份**：定期备份数据库

## 9. 接口文档访问

- **Swagger UI**：`http://localhost:8080/swagger-ui.html`
- **API文档**：`http://localhost:8080/v3/api-docs`

---

此文档为前端开发提供了完整的后端API参考，如有疑问请参考源码或联系后端开发人员。